# Hearloop Context Pack

Drop these markdown files into `docs/ai-context/` or `.cursor/` and keep them current. Use `SESSION_BOOTSTRAP.md` at the start of a new Cursor/chat session. Do not ask the model to reread the full repo unless the task requires it.

Suggested placement:

- `.cursor/SESSION_BOOTSTRAP.md`
- `.cursor/PROJECT_CONTEXT.md`
- `.cursor/ARCHITECTURE.md`
- `.cursor/AI_BEDROCK_RUNBOOK.md`
- `.cursor/KNOWN_ISSUES.md`
- `.cursor/CODE_STYLE.md`
- `.cursor/RESUME_METRICS.md`
- `.cursor/FILE_INVENTORY.md`

Your existing `.cursor` files are useful but stale. Replace or update them; they currently describe Claude direct API, `X-API-Key`, wrong widget paths, and simplified flows that no longer match the code.
