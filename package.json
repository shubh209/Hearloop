{
  "name": "hearloop",
  "private": true,
  "packageManager": "npm@11.6.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check:browser-secrets": "bash scripts/check-browser-secret-examples.sh"
  },
  "devDependencies": {
    "turbo": "^2.9.14",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
